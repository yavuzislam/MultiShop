@model List<ResultFeatureDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Öne Çıkan Alan Adı</th>
                                <th>Sil</th>
                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@item.Title</th>
                                    <td><a href="/Admin/Feature/DeleteFeature/@item.FeatureID" class="btn btn-danger">Sil</a></td>
                                    <td><a href="/Admin/Feature/UpdateFeature/@item.FeatureID" class="btn btn-success">Güncelle</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/Feature/CreateFeature" class="btn btn-primary">Yeni Öne Çıkan Alan Girişi</a>
                </div>
            </div>
        </div>
    </div>
</section>


